{
    "name": "terminal-dev-server",
    "type": "module",
    "scripts": {
        "installation": "pnpm i --frozen-lockfile"
    },
    "dependencies": {
        "express": "^4.17.3"
    },
    "devDependencies": {
        "esbuild": "^0.14.36"
    }
}
